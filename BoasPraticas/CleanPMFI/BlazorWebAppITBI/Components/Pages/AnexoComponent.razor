
if(Anexo != null){

<FluentTextField ReadOnly @bind-value="@Anexo" style="width: 10%;" Maxlength="14"></FluentTextField>
}

@code {

    [Parameter]
    public Anexo Anexo { get; set; }
  /*  //[Parameter]
    public EventCallback<TItem> OnEdit { get; set; }
   * 
    [Parameter]
    public EventCallback<TItem> OnDelete { get; set; }

    private void EditItem(TItem item)
    {
        OnEdit.InvokeAsync(item);
    }

    private void DeleteItem(TItem item)
    {
        OnDelete.InvokeAsync(item);
    }*/ 
}

@model IEnumerable<Domain.Entities.TipoProcesso>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiasTramite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Permanencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.liberado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiasTramite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Permanencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.liberado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoProcessoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoProcessoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoProcessoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
